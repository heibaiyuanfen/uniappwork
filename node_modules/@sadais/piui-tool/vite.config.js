// vite.config.js
const path = require('path')
const { defineConfig } = require('vite')

module.exports = defineConfig({
  define: {
    global: {
      uni: "uni"
    }
},
  build: {
    lib: {
      entry: path.resolve(__dirname, 'src/lib/index.js'),
      name: 'piui',
      fileName: (format) => `piui.${format}.js`
    }
  }
})
